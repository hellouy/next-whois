Good — IANA currently has 1198 gTLDs in their RDAP bootstrap, but the current file only has ~1128. Let me also check the WHOIS server source, then regenerate both files:
Good — there's already a script for RDAP. Let me run it to get the full 1198 gTLDs, then build the WHOIS server list from scratch:
RDAP is already up to date (1128 = all non-ccTLD entries from IANA's 1198). The difference is the ccTLDs already handled separately. Now let me build the WHOIS server bootstrap:
Now I have a clear picture. Let me write a comprehensive script to build both files — I'll map the 451 Identity Digital, 90 CentralNic, 84 Nominet, etc. TLDs to their correct WHOIS servers:
Now I have everything I need. Let me write the comprehensive script to build the WHOIS bootstrap:
All 307 rdap.nic.* hosts are exactly 1:1 with their TLD — perfect for pattern mapping. Now I'll write the full generator script and run it:
1189 entries generated. Now I need to wire it into the lookup code so it's actually used before falling back to dynamic discovery:
Line 162 in whoiser: let host = options?.host || cacheTldWhoisServer[domainTld] — passing host in options bypasses the IANA discovery entirely. Now I'll wire in the bootstrap:
Now integrate the WHOIS bootstrap into lookup.ts:
Now wire it into the actual whoisDomain call to pass the host when known:
Now let me verify the generated file is correct and check for TypeScript errors:
App is running cleanly with no TypeScript errors. Let me do a final verification:
App is running cleanly with no errors. Here's a complete summary of what was done:

两个文件全面扩充完成：

① RDAP bootstrap (rdap_gtld_bootstrap.ts) — 已通过现有脚本重新生成，1128 个 gTLD（IANA 总共 1198 条，其余 70 个是由 rdap_client.ts 单独管理的 ccTLD，不重叠）

② WHOIS bootstrap (whois_gtld_bootstrap.ts) — 全新文件，1185 个 gTLD 的 WHOIS 服务器，通过 IANA RDAP 数据推导而来：

注册局	TLD 数量	WHOIS 服务器
Identity Digital (原 Donuts)	452	whois.identity.digital
CentralNic	90	whois.centralnic.com
Nominet	83	whois.nominet.uk
GMO Registry	47	whois.gmoregistry.net
Google Registry	46	whois.nic.google
307 个独立 nic.* 注册局	307	whois.nic.{tld}
…等		
③ 查询逻辑已更新 (lookup.ts) — 在调用 whoiser 之前，先查本地 bootstrap。对于 1185 个已知 gTLD，直接传入 host 参数，完全跳过 IANA 实时查询，只有真正陌生的 TLD 才会走动态发现路径。